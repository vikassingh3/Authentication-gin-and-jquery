package render

/*

import (
	"net/http"

	"github.com/gin-gonic/gin"
)

func Home(c *gin.Context) {

	c.HTML(http.StatusOK, "./temp/index.html", gin.H{
		"Title": "login page",
	})
}

func PageUpdate(c *gin.Context) {

	c.HTML(http.StatusOK, "./temp/form3.html", gin.H{
		"Title": "update or delete",
	})
}

func UserRegister(c *gin.Context) {

	c.HTML(http.StatusOK, "./temp/form.html", gin.H{
		"Title": "update or delete",
	})
}
*/
